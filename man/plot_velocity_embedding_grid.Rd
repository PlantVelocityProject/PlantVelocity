% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/velocity_grid.R
\name{plot_velocity_embedding_grid}
\alias{plot_velocity_embedding_grid}
\title{Plot Velocity Embedding on a Regular Grid}
\usage{
plot_velocity_embedding_grid(
  pv,
  reduction = "umap",
  vkey = "velocity",
  group_by = NULL,
  n_grid = 30L,
  n_neighbors = NULL,
  smooth = 0.5,
  min_mass = 0.5,
  cutoff_perc = 5,
  x_pad = 0.01,
  y_pad = 0.01,
  arrow_scale = 0.8,
  arrow_min_length = 0.008,
  arrow_max_length = 0.035,
  arrow_size = 0.06,
  arrow_linewidth = 0.2,
  arrow_alpha = 0.9,
  arrow_angle = 20,
  arrow_type = c("closed", "open"),
  point_size = 0.5,
  alpha = 0.9,
  point_shape = 16,
  raster_points = TRUE,
  raster_arrows = FALSE,
  raster_dpi = 1200,
  input_color = NULL,
  title = NULL,
  legend = TRUE,
  fixed_aspect = FALSE
)
}
\arguments{
\item{pv}{A \code{plantvelo} object.}

\item{reduction}{Character scalar. Embedding name, such as \code{"umap"},
\code{"pca"}, or \code{"tsne"}. Default \code{"umap"}.}

\item{vkey}{Character scalar. Velocity layer key. Default \code{"velocity"}.}

\item{group_by}{Character scalar naming a metadata column, a numeric vector,
or \code{NULL}, using the same colour semantics as
\code{plot_velocity_embedding()}. Default \code{NULL}.}

\item{n_grid}{Integer. Number of regular grid positions along each axis.
Default \code{30L}.}

\item{n_neighbors}{Integer or \code{NULL}. Number of nearest cells used to
estimate velocity at each grid position. If \code{NULL}, an automatic
value is used.}

\item{smooth}{Positive numeric. Gaussian bandwidth as a multiple of the mean
grid spacing. Default \code{0.5}.}

\item{min_mass}{Numeric. Lower-bound control for the interpolated
velocity-strength mask. Default \code{1}.}

\item{cutoff_perc}{Numeric in \eqn{[0,100]}. Percentile threshold applied to
local velocity length. Default \code{5}.}

\item{x_pad, y_pad}{Non-negative numeric. Fractional padding added to the
embedding ranges before constructing the grid. Default \code{0.01}.}

\item{arrow_scale}{Non-negative numeric. Global arrow-length multiplier.
Default \code{0.8}.}

\item{arrow_min_length, arrow_max_length}{Non-negative numeric. Minimum and
maximum visible arrow lengths as fractions of the total embedding span.
The maximum must be at least the minimum. Defaults are \code{0.008} and
\code{0.035}.}

\item{arrow_size}{Non-negative numeric. Arrow-head length in cm. Default
\code{0.06}.}

\item{arrow_linewidth}{Non-negative numeric. Arrow shaft linewidth. Default
\code{0.15}.}

\item{arrow_alpha}{Numeric in \eqn{[0,1]}. Arrow opacity. Default \code{0.90}.}

\item{arrow_angle}{Numeric strictly between 0 and 90. Arrow-head half-angle
in degrees. Default \code{20}.}

\item{arrow_type}{Character. Arrow-head type, either \code{"closed"} or
\code{"open"}. Default \code{"closed"}.}

\item{point_size}{Non-negative numeric. Cell-point size. Default \code{0.25}.}

\item{alpha}{Numeric in \eqn{[0,1]}. Cell-point opacity. Default \code{0.90}.}

\item{point_shape}{Finite numeric. Cell-point shape. Default \code{16}.}

\item{raster_points}{Logical. Rasterize the cell-point layer with
\pkg{ggrastr}. Default \code{TRUE}.}

\item{raster_arrows}{Logical. Rasterize the grid-arrow layer with
\pkg{ggrastr}. Default \code{FALSE}.}

\item{raster_dpi}{Positive numeric. Resolution in dots per inch for
rasterized layers. Default \code{1200}.}

\item{input_color}{Character vector or \code{NULL}. Custom discrete or
continuous colour palette for cells. Default \code{NULL}.}

\item{title}{Character scalar or \code{NULL}. Plot title. Default
\code{NULL}.}

\item{legend}{Logical. Whether to show the legend. Default \code{TRUE}.}

\item{fixed_aspect}{Logical. Whether to use an equal x/y coordinate ratio.
Default \code{FALSE}.}
}
\value{
A \code{ggplot} object.
}
\description{
Plot cells in a two-dimensional embedding overlaid with locally averaged
RNA velocity arrows on a regular grid. Cell points and grid arrows can be
rasterized independently while axes, legends, and text remain vector
graphics.
}
\details{
Grid velocities are estimated with the same Gaussian nearest-neighbour
interpolation and masking used by \code{plot_velocity_stream()}, but this
function draws one arrow per valid grid position and does not calculate or
cache streamlines. Arrow magnitudes are clipped at their 95th percentile,
square-root normalized, and mapped to the requested visible length range.
}
