% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/terminal_states.R
\name{compute_cell_fate}
\alias{compute_cell_fate}
\title{Compute Cell Fate Assignment}
\usage{
compute_cell_fate(
  pv,
  groupby = "clusters",
  disconnected_groups = NULL,
  self_transitions = FALSE,
  verbose = TRUE
)
}
\arguments{
\item{pv}{A \code{plantvelo} object.}

\item{groupby}{Character scalar. Column in \code{@meta.data} containing
cell-state or cluster labels. Default \code{"clusters"}.}

\item{disconnected_groups}{Character vector or \code{NULL}. Groups excluded
from fate computation. Cells in these groups retain their own label.
Default \code{NULL}.}

\item{self_transitions}{Logical. Add self-loops before transition
normalisation. Default \code{FALSE}.}

\item{verbose}{Logical. Print progress messages. Default \code{TRUE}.}
}
\value{
A \code{plantvelo} object with \code{@meta.data$cell_fate} and
\code{@meta.data$cell_fate_confidence} added.
}
\description{
Assign each cell to the most likely terminal-state group using the
fundamental matrix \eqn{N = (I - T)^{-1}}.

\strong{Warning}: matrix inversion is \eqn{O(n^3)} and is only recommended
for datasets with fewer than about 3,000 cells.
}
