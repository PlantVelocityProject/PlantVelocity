% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/create_plantvelo.R
\docType{class}
\name{plantvelo-class}
\alias{plantvelo-class}
\title{plantvelo S4 Class}
\description{
Container for RNA velocity analysis of plant single-cell data.
Stores raw count layers, smoothed moments, kinetic parameters, velocity
vectors, and all derived graphs in a single object.
}
\section{Slots}{

\describe{
\item{\code{assays}}{Named list of Seurat assay objects (typically \code{RNA}).}

\item{\code{meta.data}}{data.frame of per-cell metadata (one row per cell).}

\item{\code{layers}}{Named list of genes x cells matrices: \code{spliced},
\code{unspliced}, \code{ir} (optional intron-retained), \code{velocity},
\code{velocity_u}, \code{fit_t}, \code{fit_tau}, etc.}

\item{\code{reductions}}{Named list of dimensionality reductions (Seurat
\code{DimReduc} objects or plain matrices).  Includes PCA, UMAP/tSNE, and
velocity embeddings (e.g. \code{velocity_umap}).}

\item{\code{active.ident}}{Factor of cell identities (mirrors Seurat
\code{Idents()}).}

\item{\code{graphs}}{Named list of graph objects: \code{neighbors} (KNN graph with
\code{connectivities} and \code{distances}), \code{velocity_graph},
\code{velocity_graph_neg}.}

\item{\code{moments}}{Named list of cells x genes smoothed expression matrices:
\code{Ms} (spliced), \code{Mu} (unspliced), and optional \code{Mr} (IR).}

\item{\code{kinetics}}{Named list; \code{params} is a genes x parameters data.frame
of EM-fitted kinetic rates from \code{recover_dynamics()}.}

\item{\code{velocity}}{Named list; \code{genes} is a named logical vector of
high-quality velocity genes; \code{stream} stores the stream-field
data.frame from \code{compute_velocity_stream()}.}

\item{\code{misc}}{Named list for miscellaneous metadata: run parameters, IR gene
statistics (\code{ir_gene_stats}), driver gene statistics
(\code{velocity_gene_stats}), and merged-object bookkeeping.}
}}

