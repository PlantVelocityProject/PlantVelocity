% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/velocity_graph.R
\name{get_cell_transitions}
\alias{get_cell_transitions}
\title{Simulate Cell Trajectory by Random Walk}
\usage{
get_cell_transitions(
  pv,
  starting_cell,
  n_steps = 10,
  vkey = "velocity",
  random_state = NULL
)
}
\arguments{
\item{pv}{A \code{plantvelo} object with a velocity graph already computed.}

\item{starting_cell}{Integer. Starting cell index (1-based).}

\item{n_steps}{Integer. Number of random-walk steps. Default \code{10}.}

\item{vkey}{Character scalar. Velocity layer key. Default
\code{"velocity"}.}

\item{random_state}{Integer or \code{NULL}. Random seed. Default
\code{NULL}.}
}
\value{
Integer vector of length \code{n_steps + 1} containing the visited
cell indices along the random-walk path.
}
\description{
Simulate a directed random walk on the velocity transition matrix, starting
from a specified cell.

At each step, the next cell is sampled from the row of the transition matrix
corresponding to the current cell.
}
\examples{
\dontrun{
path <- get_cell_transitions(pv, starting_cell = 1, n_steps = 20)
}
}
