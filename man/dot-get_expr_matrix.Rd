% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compute_velocity.R
\name{.get_expr_matrix}
\alias{.get_expr_matrix}
\title{Retrieve and align an expression matrix (cells × genes)}
\usage{
.get_expr_matrix(pv, moment_key, layer_key, use_raw, required = FALSE)
}
\arguments{
\item{pv}{A \code{plantvelo} object.}

\item{moment_key}{Character scalar. Name of the matrix in \code{@moments}.}

\item{layer_key}{Character scalar. Name of the matrix in \code{@layers}.}

\item{use_raw}{Logical. Whether to force use of raw layers.}

\item{required}{Logical. Whether absence of both sources is an error.}
}
\value{
A dense numeric matrix of cells \eqn{\times} genes, or \code{NULL}.
}
\description{
Prefer the smoothed moment in \code{@moments} specified by
\code{moment_key}. If unavailable, fall back to the raw layer in
\code{@layers} specified by \code{layer_key}, transposing it from
genes \eqn{\times} cells to cells \eqn{\times} genes. Named axes are
strictly matched and reordered to canonical spliced-layer names; unnamed
axes are accepted only at the exact canonical dimensions.
}
\details{
Returns \code{NULL} if neither source is available.
}
\keyword{internal}
