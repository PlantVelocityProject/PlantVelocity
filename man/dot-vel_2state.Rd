% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compute_velocity.R
\name{.vel_2state}
\alias{.vel_2state}
\title{Compute two-state dynamical velocity}
\usage{
.vel_2state(pv, min_likelihood, use_raw, vkey, verbose)
}
\arguments{
\item{pv}{A \code{plantvelo} object with two-state kinetic parameters in
\code{@kinetics$params}.}

\item{min_likelihood}{Numeric. Minimum likelihood threshold used for
velocity gene filtering.}

\item{use_raw}{Logical. Use raw layers instead of smoothed moments.}

\item{vkey}{Character scalar. Base name for the output velocity layers.}

\item{verbose}{Logical. Print progress messages.}
}
\value{
A \code{plantvelo} object with \code{@layers$velocity},
\code{@layers$velocity_u}, and \code{@velocity$genes} updated.
}
\description{
Implements the scVelo-compatible U/S velocity equations, consistent with
\code{BaseDynamics$get_vt()} and \code{get_wt()} in the
\code{use_ir = FALSE} branch:
\itemize{
\item \code{vt = β·U(t) - γ·S_obs}
  \item \code{wt = (α·o - β·U(t)) · scaling}
}
}
\details{
If \code{fit_t} is available, \code{U(t)} is computed from the ODE solution.
Otherwise, the scaled observed unspliced expression is used directly.
}
\keyword{internal}
